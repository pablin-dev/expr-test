# This rule blocks transactions from emails in the blacklist.
rule:
  name: "Block blacklisted emails"
  condition:
    condition:
      attribute: "user.email"
      operator: "in"
      value:
        - "bad.email@example.com"
        - "another.bad.email@example.com"
        - "spam@domain.com"
